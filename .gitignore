# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files (NEVER commit real keys)
.env
.env.local
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# vercel
.vercel

# 시드 계정 (비밀번호 포함 — 절대 커밋 금지)
scripts/accounts.json

# Claude Code 로컬 설정 (머신별 — 커밋 제외)
.claude/settings.local.json
